<?php

/**
 * Implements hook_preprocess_page().
 */
function netmidas_preprocess_page(&$variables) {
  // Load this library only on front page.
  if ($variables['is_front']) {
    $variables['#attached']['library'][] = 'netmidas/front-page';
  }
}
